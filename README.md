# 莎莎的个人看板 · EdgeOne

这是 `shasha-personal-dashboard` 的腾讯云 EdgeOne Makers 部署分支。

- `main`：ChatGPT Sites / Vinext 源码镜像。
- `edgeone`：标准 Vite 静态站，供 EdgeOne Makers 自动构建。

EdgeOne 构建配置由根目录的 `edgeone.json` 提供：执行 `npm ci`、`npm run build`，发布 `dist`。
